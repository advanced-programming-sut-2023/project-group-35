package model.buildings;

import model.Reign;
import Enum.*;
public class Wall extends Building {
    public Wall(Reign owner, BuildingType buildingType) {
        super(owner, buildingType);
    }
}
