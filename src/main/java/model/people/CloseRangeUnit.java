package model.people;

import Enum.*;
import model.*;
public class CloseRangeUnit extends MilitaryUnit{
    public CloseRangeUnit(UnitType unitType, Reign owner, Block block, int number, int hp) {
        super(unitType, owner, block, number, hp);
    }
}
