package model.people;

import Enum.*;
import model.*;
public class CloseRangeUnit extends MilitaryUnit{
    public CloseRangeUnit(Reign owner, UnitType unitType, int number, int hp) {
        super(owner, unitType, number, hp);
    }
}
